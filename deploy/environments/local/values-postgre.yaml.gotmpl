global:
    postgresql:
        auth:
            postgresPassword: postgres

primary:
    service:
        type: NodePort
        nodePorts:
            postgresql: 30100

    persistence:
        size: 5Gi
